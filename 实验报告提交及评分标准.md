# 8. Agent 运行时治理：异步并发、超时重试与 Token 预算控制 - 实验报告提交及评分标准

## 一、实验报告基本结构

- **实验名称**：Agent 运行时治理：异步并发、超时重试与 Token 预算控制
- **学生姓名**：
- **学号**：
- **班级**：
- **实验日期**：
- **指导教师**：

## 二、实验报告目录

1. 实验目的与运行时目标契约
2. 实验环境与平台说明
3. 请求协议、结果协议与运行时策略
4. 并发、超时与重试机制
5. Token 估算与预算控制
6. 默认压测结果与 JSONL 日志分析
7. pytest 自动化测试结果
8. Docker 复现情况或环境限制说明
9. 失败反馈与文档修正建议
10. 实验总结与心得体会

## 三、提交材料

1. 实验报告文档（Markdown、Word 或 PDF）。
2. Python 源码文件：
   - `runtime_controller.py`
   - `demo_benchmark.py`
   - `tests/test_runtime_controller.py`
   - `queue_demo.py` 或拒绝/排队策略观察脚本
3. `python demo_benchmark.py` 的终端输出。
4. `python -m pytest -q` 的终端输出。
5. `logs/runtime.jsonl` 中至少 5 行脱敏日志样例。
6. 至少一条超时、重试或预算拒绝结果的分析。
7. Docker 验证结果；如果 Docker daemon 不可用，应说明环境原因和替代验证路径。

## 四、评分标准

| 评分项 | 分值 | 评分要点 |
| --- | ---: | --- |
| 运行时目标契约 | 10 | 能说明并发、超时、重试、预算和日志的成功标准 |
| 请求与结果协议 | 10 | `AgentRequest`、`AgentResult` 字段清晰，失败结果结构化 |
| 并发与超时重试 | 20 | 能使用信号量限制并发，使用超时和有限重试处理慢请求/临时失败 |
| Token 预算控制 | 20 | 能实现短窗口预算、拒绝策略，并能解释与排队策略的差异 |
| JSONL 日志与压测分析 | 15 | 能记录状态、耗时、Token 和错误类型，并分析默认压测结果 |
| 自动化测试 | 15 | `python -m pytest -q` 通过，且包含快速完成、预算拒绝、超时失败测试 |
| 跨平台与 Docker 说明 | 5 | 能说明 macOS/Linux/Windows 或 Docker 环境差异 |
| 报告表达 | 5 | 能清楚讨论性能、成本、稳定性之间的权衡 |

## 五、加分项

- 将真实本地模型或 OpenAI 兼容 API 接入运行时控制器，并保留预算与日志：+5 分。
- 增加 CSV/图表展示并发数、耗时和拒绝率关系：+4 分。
- 为排队模式增加后台任务状态或进度提示：+4 分。

## 六、注意事项

1. 不要在报告、截图、日志中展示完整 API Key、Token 或内部服务地址。
2. 默认实验必须能在短时间内完成；不要把“等待 60 秒”作为课堂默认路径。
3. Docker 失败时应先判断 daemon、镜像网络和权限问题，不要直接修改业务代码。
4. 报告必须包含失败案例分析，不能只展示成功输出。

## 七、常见问题

### Q1: 不接真实模型是否可以？

可以。本实验核心是运行时治理能力，模拟模型足以验证并发、超时、重试、预算和日志。

### Q2: 为什么测试里要求默认流程小于 10 秒？

这是手册可执行性的底线。默认路径如果容易超时或卡住，课堂就无法稳定复现。

### Q3: 如果我的 Docker 无法运行怎么办？

先完成 conda 或 venv 路径，并记录 `docker --version`、daemon 状态和失败原因。Docker 不可用属于环境限制，但需要给出替代验证证据。
